name: Ros_Bin_Auto_Trigger
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/openwrt.yml'
      - '.github/workflows/ros.bin'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 检出仓库
        uses: actions/checkout@v4

      - name: 安装必需工具（含修复依赖）
        run: sudo apt update -y && sudo apt install -y git build-essential zlib1g-dev liblzma-dev liblzo2-dev squashfs-tools u-boot-tools binwalk wget flex bison libssl-dev

      - name: 彻底清理残留
        run: sudo rm -rf fmk* fmk_ros_only* ros.extracted ros.bin.extracted final_fw.bin 2>/dev/null

      - name: 克隆FMK工具并编译
        run: |
          git clone --depth 1 https://github.com/rampageX/firmware-mod-kit.git fmk_ros_only
          cd fmk_ros_only/src
          sudo make clean && sudo make || { echo "ERROR: FMK编译失败"; exit 1; }
          cd ../../ && chmod +x fmk_ros_only/*.sh

      - name: 定义固件文件名（匹配上传的ros.bin）
        run: echo "FW_FILE=ros.bin" >> $GITHUB_ENV

      - name: 检查并复制固件（从.github/workflows/目录读取）
        run: |
          FW_PATH=".github/workflows/$FW_FILE"
          if [ -f "$FW_PATH" ]; then
            cp $FW_PATH ./$FW_FILE
            echo "✅ 成功找到并复制固件：$FW_PATH"
          else
            echo "ERROR: 未找到$FW_PATH，请确认固件在.github/workflows/目录下"
            exit 1
          fi

      - name: 自动扫描固件参数（彻底修复变量为空问题）
        run: |
          # 初始化默认参数（优先保证变量非空）
          IMAGE_ARCH="mips"
          IMAGE_ADDR="0x80060000"
          IMAGE_ENTRY="0x80060000"
          IMAGE_COMP="lzma"
          SQSH_COMP="xz"
          SQSH_BLOCK="262144"

          # 扫描uImage头部参数（适配不同mkimage输出格式）
          if mkimage -l $FW_FILE 2>/dev/null; then
            # 提取架构（兼容Arch/Architecture关键词）
            ARCH_LINE=$(mkimage -l $FW_FILE | grep -Ei "Arch|Architecture")
            if [ -n "$ARCH_LINE" ]; then
              IMAGE_ARCH=$(echo $ARCH_LINE | awk '{print $NF}' | sed 's/://g' | tr -d ' ' | tr '[:upper:]' '[:lower:]')
            fi
            # 提取加载地址（兼容Data Address/Load Address）
            ADDR_LINE=$(mkimage -l $FW_FILE | grep -Ei "Data Address|Load Address")
            if [ -n "$ADDR_LINE" ]; then
              IMAGE_ADDR=$(echo $ADDR_LINE | awk '{print $NF}' | tr -d ' ')
            fi
            # 提取入口地址
            ENTRY_LINE=$(mkimage -l $FW_FILE | grep -Ei "Entry Point")
            if [ -n "$ENTRY_LINE" ]; then
              IMAGE_ENTRY=$(echo $ENTRY_LINE | awk '{print $NF}' | tr -d ' ')
            fi
            # 提取压缩格式
            COMP_LINE=$(mkimage -l $FW_FILE | grep -Ei "Compression")
            if [ -n "$COMP_LINE" ]; then
              IMAGE_COMP=$(echo $COMP_LINE | awk '{print $NF}' | sed 's/://g' | tr -d ' ' | tr '[:upper:]' '[:lower:]')
            fi
          fi

          # 扫描Squashfs参数（修复字符污染）
          if binwalk $FW_FILE | grep -i "Squashfs filesystem" 2>/dev/null; then
            # 提取压缩格式
            SQSH_COMP_LINE=$(binwalk $FW_FILE | grep -i "Squashfs filesystem" | grep -i "compression")
            if [ -n "$SQSH_COMP_LINE" ]; then
              SQSH_COMP=$(echo $SQSH_COMP_LINE | sed -E 's/.*compression: *([a-z0-9]+).*/\1/' | tr -d ' ')
            fi
            # 提取块大小
            SQSH_BLOCK_LINE=$(binwalk $FW_FILE | grep -i "Squashfs filesystem" | grep -i "blocksize")
            if [ -n "$SQSH_BLOCK_LINE" ]; then
              SQSH_BLOCK=$(echo $SQSH_BLOCK_LINE | sed -E 's/.*blocksize: *([0-9]+).*/\1/' | tr -d ' ')
            fi
          fi

          # 最终参数校验（确保关键参数有效）
          IMAGE_ARCH=${IMAGE_ARCH:-"mips"}
          IMAGE_ADDR=${IMAGE_ADDR:-"0x80060000"}
          IMAGE_ENTRY=${IMAGE_ENTRY:-"0x80060000"}
          IMAGE_COMP=${IMAGE_COMP:-"lzma"}
          SQSH_COMP=${SQSH_COMP:-"xz"}
          SQSH_BLOCK=${SQSH_BLOCK:-"262144"}

          # 输出参数（方便调试）
          echo "📌 固件参数："
          echo "架构：$IMAGE_ARCH | 加载地址：$IMAGE_ADDR | 入口地址：$IMAGE_ENTRY | 内核压缩：$IMAGE_COMP"
          echo "文件系统压缩：$SQSH_COMP | 块大小：$SQSH_BLOCK"

          # 存入环境变量
          echo "IMAGE_ARCH=$IMAGE_ARCH" >> $GITHUB_ENV
          echo "IMAGE_ADDR=$IMAGE_ADDR" >> $GITHUB_ENV
          echo "IMAGE_ENTRY=$IMAGE_ENTRY" >> $GITHUB_ENV
          echo "IMAGE_COMP=$IMAGE_COMP" >> $GITHUB_ENV
          echo "SQSH_COMP=$SQSH_COMP" >> $GITHUB_ENV
          echo "SQSH_BLOCK=$SQSH_BLOCK" >> $GITHUB_ENV

      - name: 自动解包ros.bin
        run: |
          sudo ./fmk_ros_only/extract-firmware.sh $FW_FILE
          if [ -d "$FW_FILE.extracted/rootfs" ]; then
            mv $FW_FILE.extracted ros.extracted
          elif [ -d "fmk/rootfs" ]; then
            mv fmk ros.extracted
          else
            echo "ERROR: 固件解包失败，确认是uImage+Squashfs格式！"
            exit 1
          fi

      - name: 自动打包rootfs
        run: |
          cd ros.extracted/rootfs
          sudo mksquashfs . ../rootfs.sqsh -b $SQSH_BLOCK -comp $SQSH_COMP -no-xattrs -all-root
          cd ../../

      - name: 重建uImage头部（添加镜像名，兼容所有mkimage版本）
        run: |
          sudo mkimage -A $IMAGE_ARCH -O linux -T kernel -C $IMAGE_COMP -a $IMAGE_ADDR -e $IMAGE_ENTRY -n "ROS_Firmware" -d ros.extracted/rootfs.sqsh final_fw.bin

      - name: 上传修改后的固件
        uses: actions/upload-artifact@v4
        with:
          name: Modified_ROS_Firmware
          path: final_fw.bin
          retention-days: 30
